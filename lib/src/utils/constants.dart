import 'package:flutter/widgets.dart';

class Constants {
  static const double arrowHeight = 8.0;
  static const double arrowWidth = 12.0;
  static const double overlayContainerMaxWidth = 200;
  static const fillBackGroundBalloonTipColor = Color.fromRGBO(0, 0, 0, 0.85);

  static const EdgeInsets overlayContainerPadding = EdgeInsets.symmetric(
    horizontal: 16.0,
    vertical: 12.0,
  );
}
